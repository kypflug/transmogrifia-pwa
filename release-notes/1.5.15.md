# Library of Transmogrifia 1.5.15

## Fixed

- Completed cloud generation jobs no longer persist in the pending list after a page reload. Previously, the poll-based completion check only looked for new articles (count increase) rather than checking whether the article already existed, causing restored jobs to linger until the 10-minute timeout.
